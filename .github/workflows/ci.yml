name: CI Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  # Backend Tests
  backend:
    name: 🔧 Backend (NestJS)
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔎 Check for backend package.json
        id: check-backend
        working-directory: apps/backend
        run: |
          if [ ! -f package.json ]; then
            echo "skip=true" >> $GITHUB_OUTPUT
          fi

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          # FIX: Only cache if package-lock.json exists
          cache: ${{ hashFiles('apps/backend/package-lock.json') != '' && 'npm' || '' }}
          cache-dependency-path: 'apps/backend/package-lock.json'

      - name: 📦 Install dependencies
        if: steps.check-backend.outputs.skip != 'true'
        working-directory: apps/backend
        run: npm ci || npm install

      - name: 🔍 Lint
        if: steps.check-backend.outputs.skip != 'true'
        working-directory: apps/backend
        run: npm run lint || echo "⚠️ No lint script"

      - name: 🏗️ Build
        if: steps.check-backend.outputs.skip != 'true'
        working-directory: apps/backend
        run: npm run build

      - name: ✅ Backend complete
        run: echo "✅ Backend checks passed"

  # Frontend Tests
  frontend:
    name: ⚛️ Frontend (React)
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔎 Check for frontend package.json
        id: check-frontend
        working-directory: apps/frontend
        run: |
          if [ ! -f package.json ]; then
            echo "skip=true" >> $GITHUB_OUTPUT
          fi

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          # FIX: Only cache if package-lock.json exists
          cache: ${{ hashFiles('apps/frontend/package-lock.json') != '' && 'npm' || '' }}
          cache-dependency-path: 'apps/frontend/package-lock.json'

      - name: 📦 Install dependencies
        if: steps.check-frontend.outputs.skip != 'true'
        working-directory: apps/frontend
        run: npm ci || npm install

      - name: 🔍 Lint
        if: steps.check-frontend.outputs.skip != 'true'
        working-directory: apps/frontend
        run: npm run lint || echo "⚠️ No lint script"

      - name: 🏗️ Build
        if: steps.check-frontend.outputs.skip != 'true'
        working-directory: apps/frontend
        run: npm run build

      - name: ✅ Frontend complete
        run: echo "✅ Frontend checks passed"

  # Docker Build
  docker:
    name: 🐳 Docker Build
    runs-on: ubuntu-latest
    needs: [backend, frontend]
    # Always run, even if backend/frontend skipped
    if: always()
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔍 Verify docker-compose
        run: |
          if [ -f "docker-compose.yml" ]; then
            echo "✅ docker-compose.yml exists"
          else
            echo "❌ docker-compose.yml missing"
            exit 1
          fi

      - name: ✅ Docker checks complete
        run: echo "✅ Docker configuration verified"
