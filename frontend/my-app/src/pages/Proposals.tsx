/* Placeholder — Heba's module */
export default function Proposals() {
  return (
    <div style={{ animation: 'fadeUp .3s var(--ease) both' }}>
      <h2
        style={{
          fontFamily: 'var(--font-d)',
          fontWeight: 700,
          fontSize: 26,
          color: 'var(--white)',
          letterSpacing: '.01em',
          lineHeight: 1.3,
          marginBottom: 4,
        }}
      >
        Proposals
      </h2>
      <p style={{ fontFamily: 'var(--font-m)', fontSize: 11, color: 'var(--text-dim)', letterSpacing: '.04em' }}>
        Coming soon — this module will be built by Heba.
      </p>
    </div>
  );
}
